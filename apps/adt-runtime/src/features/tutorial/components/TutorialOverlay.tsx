/**
 * Tutorial overlay — stub.
 *
 * Legacy `tutorial.js` (568 LOC) walks the chrome with positioned popovers
 * highlighting each affordance on first visit. Porting it requires per-step
 * anchor refs across multiple components plus a Popover with custom
 * positioning. Tracked as a follow-up; this stub renders nothing so the
 * boot lifecycle can wire it in without conditional imports.
 */
export function TutorialOverlay() {
  return null
}

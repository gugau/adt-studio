/**
 * Zoom controller — stub.
 *
 * Legacy `browser_zoom_controller.js` detects high-DPI Windows installs and
 * applies a CSS-based zoom adjustment, plus exposes manual zoom controls in
 * the sidebar. To keep Phase A+B focused, the hook is a no-op for now;
 * the real implementation will land alongside the sidebar's zoom controls.
 */
export function useZoomController(): void {
  // Intentionally empty — see file header.
}
